.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Corpora and generated splits are never committed. The postings are yours,
# the demo set is regenerated by `python -m relevance.demo`, and a split that
# ships in the repo is a split CI would refuse to re-freeze -- correctly.
data/
table.txt
